IMECE `C2` condition.

Use the same generic ROS interface surface as `C1`.

Only the frozen helper subset selected from discovery may be used, and each helper remains non-semantic:
- transport/safety support only
- no automatic mission generation
- no semantic takeoff/goto/return-home primitives
